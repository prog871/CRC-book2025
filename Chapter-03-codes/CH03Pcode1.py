# Define the function
def f(x):
    return x + 1
# Define the point of interest
x0 = 0
# Calculate the limit
limit_value = f(x0)
# Display the result
print("The limit of f(x) = x + 1 as x \
approaches 0 is %.2f" % limit_value)
